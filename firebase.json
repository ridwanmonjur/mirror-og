{
  "firestore": [
   
    {
      "database": "driftwood-dev",
      "rules": "firestore.rules"
    },
    {
      "database": "driftwood-staging", 
      "rules": "firestore.rules"
    },
    {
      "database": "driftwood-prod",
      "rules": "firestore.rules"
    }
  ]
}
