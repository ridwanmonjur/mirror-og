# Exclude everything by default
*

# Then include only what we need
!terraform/
!.env
!.env.prod
!.env.staging
!.env.example
!.env.localdocker
!firebase/
!composer.json
!firestore.rules
!cloud_server_functions/
!cloud_client_auth/
!package.json
!composer.lock

# But exclude these from terraform folder
terraform/.terraform/
terraform/*.tfstate
terraform/*.tfstate.backup
terraform/*.tfplan

# Exclude node_modules and virtual environments
node_modules/
.venv/
venv/
__pycache__/
*.pyc
*.pyo
